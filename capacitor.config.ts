import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bakala.everywhare',
  appName: 'Bakala Everywhare',
  webDir: 'dist'
};

export default config;
